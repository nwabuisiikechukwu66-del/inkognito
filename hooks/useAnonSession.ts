/**
 * hooks/useAnonSession.ts
 *
 * Re-export of the useAnonSession hook from AnonSessionProvider.
 * Allows cleaner imports: `import { useAnonSession } from "@/hooks/useAnonSession"`
 * instead of the full provider path.
 */

export { useAnonSession } from "@/components/providers/AnonSessionProvider";
